previousButton.addEventListener("click", function () { redirect(pagesUrl[2]); });

startOverBtn.addEventListener("click", function () { redirect(pagesUrl[0]); });

closeBtn.addEventListener("click", function () { window.close(); });


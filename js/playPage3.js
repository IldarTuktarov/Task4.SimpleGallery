playButton.addEventListener("click", function () { play(pagesUrl[3]); });

stopButton.addEventListener("click", function () { stop(); });

nextButton.addEventListener("click", function () { redirect(pagesUrl[3]); });

previousButton.addEventListener("click", function () { redirect(pagesUrl[1]); });

play(pagesUrl[3]);






